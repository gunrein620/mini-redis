# Required Assets

Place the following PNG files here if you want to show service icons inside the issue buttons:

- `redis.png`
- `postgresql.png`

The current repository does not include these files, so the UI keeps text-only buttons to avoid broken image paths.
